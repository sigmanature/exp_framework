[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "exp-framework"
version = "0.1.0"
description = "Android THP/mTHP long-running experiment framework (sampling, memstress workload, device prep)"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
